# Build kontekstidan chiqariladi — image kichik va sirlar ichkariga tushmaydi.

node_modules
.next
out

# Sirlar — runtime env compose'dagi env_file orqali beriladi
.env
.env.*
!.env.example
!.env.production.example

# Baza zaxiralari (server papkasida turib qolishi mumkin)
*.sql.gz
*.dump
*.tar.gz

# Faqat dev uchun: soxta `project` bazasi va lokal sozlash skriptlari
scripts/dev

# Git / IDE / OS
.git
.gitignore
.gitattributes
.vscode
.idea
.DS_Store
Thumbs.db

# Claude / vaqtinchalik
.claude
_*.ts
*.log

# Hujjatlar (image ichida keraksiz)
*.md
!README.md
